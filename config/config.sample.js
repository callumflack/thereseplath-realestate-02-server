const config = {
	// Path of xml files to convert
	xmlPath: 'xmlfiles',
	// Path of converted xml files
	historyPath: 'history',
	// Path of github repository to push generated JSON
	jsonPath: '../git_repo',
}

module.exports = config;
